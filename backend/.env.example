PORT=4000
CORS_ORIGIN=http://localhost:5173

# Optional placeholders for future live provider integrations.
# Keep these on the backend host only; do not expose them to the frontend.
WEATHER_API_KEY=
NEWS_API_KEY=
SEARCH_API_KEY=
